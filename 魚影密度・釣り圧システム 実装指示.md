# 魚影密度・釣り圧システム 実装指示

水中探索画面に出現する魚シンボル数へゲーム性を持たせるため、**同じ場所で釣り続けるほど周辺の魚影が減っていく「釣り圧」システム**を追加してください。

## 基本仕様

- 釣り場は固定ポイント制ではなく、プレイヤーが無段階で位置を変えてキャストできる
- 将来的にマップはタイルマップへ移行予定
- 水場 / 地面などのタイル属性や当たり判定を持つ予定
- そのため、釣り圧は最終的に**水タイル単位で管理できる構造**にする
- 同じ場所で魚を釣るほど、その周辺の魚シンボル出現数を減らす
- 少し場所を移動すれば徐々に魚影が回復するような分布にする
- 時間経過でも釣り圧は減衰し、最終的には元の状態へ戻る

---

## タイル情報と釣り圧は分離する

マップそのものが持つ固定情報と、プレイ中に変化する状態は分ける。

イメージ：

```ts
type TileData = {
  type: 'water' | 'ground';
  walkable: boolean;

  // 将来的な拡張用
  depth?: number;
  habitat?: string;
  baseFishDensity?: number;
};
```

釣り圧などの変動情報は別管理にする。

```ts
type FishingTileState = {
  fishingPressure: number;
  lastUpdatedAt: number;
};
```

`TileData` 自体を書き換えるのではなく、セーブ可能なゲーム状態として管理できる構造にしてください。

---

## 魚を釣ったときの処理

魚を釣り上げた位置を基準に、その周辺へ釣り圧を加算する。

中心地点ほど強く、離れるほど弱く影響する。

概念としては、

```text
中心        強い
↓
近距離      中程度
↓
外周        弱い
↓
範囲外      影響なし
```

とする。

単純に中心セルだけを減らすのではなく、**周辺タイルにも釣り圧を伝播させる**ことで、セル境界をまたいだ際に魚影数が急変しないようにしてください。

仮実装としては、

- 中心付近：+20%
- 隣接付近：+10%
- 外周：+3%

程度から開始して構いません。

数値は後から調整できるようconfig化してください。

可能であれば、固定パターンではなく距離ベースで減衰させてください。

例：

```ts
const influence = Math.max(
  0,
  1 - distance / pressureRadius
);

pressureAdd = maxPressureAdd * influence;
```

---

## 水タイルのみに適用する

釣り圧は水場だけに付与する。

```ts
if (tile.type !== 'water') {
  return;
}
```

岸際で魚を釣った場合でも、周辺の地面タイルには釣り圧を持たせない。

将来的には、水域が分離している場合に、

```text
池A | 地面 | 池B
```

池Aで発生した釣り圧が池Bへ伝わらないよう、同一水域判定を追加できる構造にしておく。

現時点で水域ID等が存在しない場合、この機能自体は後回しで構いません。

---

## 魚シンボル生成数への反映

キャスト地点周辺の釣り圧を取得し、水中探索画面に生成する魚シンボル数へ反映する。

基本イメージ：

```ts
const densityMultiplier = 1 - fishingPressure;

const calculatedFishCount =
  baseFishCount * densityMultiplier;
```

釣り圧が低いほど魚が多く、

```text
pressure = 0.0
→ 通常の魚影数

pressure = 0.3
→ 少し魚影が少ない

pressure = 0.7
→ かなり魚影が少ない
```

という状態にする。

ただし、既存仕様として**探索画面には最低2匹の魚を保証する**。

```ts
const fishCount = Math.max(
  2,
  Math.round(calculatedFishCount)
);
```

---

## キャスト地点周辺の値を参照する

キャスト位置がセル境界付近だった場合でも不自然にならないよう、可能であればキャスト地点が属する1タイルだけではなく、**周辺タイルの釣り圧を距離加重して平均化**してください。

イメージ：

```ts
const localFishingPressure =
  getFishingPressureAround(castPosition);
```

これにより、プレイヤーが数px位置を変えただけで、

```text
魚が2匹 → 6匹
```

のような極端な変化が起きないようにする。

---

## 時間経過による回復

各タイルの `fishingPressure` は時間経過で徐々に減衰させる。

```ts
pressure -= recoveryPerSecond * elapsedTime;
pressure = Math.max(0, pressure);
```

常時全タイルを更新する必要はなく、`lastUpdatedAt` を保存しておき、参照時に経過時間から現在値を計算する方式でも構いません。

例：

```ts
function getCurrentFishingPressure(state: FishingTileState) {
  const elapsed = now - state.lastUpdatedAt;

  return Math.max(
    0,
    state.fishingPressure - elapsed * recoveryRate
  );
}
```

パフォーマンスと既存構成に合わせて実装してください。

---

## 将来的な拡張を考慮する

今後、水タイルには以下のような情報を持たせる可能性があります。

```ts
type WaterTileData = {
  depth?: number;
  habitat?: string;
  baseFishDensity?: number;
};
```

将来的には、

- 深い場所ほど大型魚が出やすい
- 水草周辺では特定魚種が出やすい
- 元々魚影が濃い場所 / 薄い場所がある
- 釣り圧が高い場所では魚影数が減る

などを組み合わせたい。

そのため、魚生成数を直接釣り圧だけで決め打ちせず、

```ts
baseFishDensity
×
fishingPressure補正
×
その他の環境補正
```

のように拡張できる設計にしてください。

---

## 現在まだタイルマップではない場合

現状のマップ実装がまだタイルベースでない場合、今回のためだけにマップ全体をタイルマップへ変更する必要はありません。

その場合は、内部的な仮想グリッドを作り、

```ts
worldPosition
→ gridX / gridY
```

へ変換して同じ釣り圧ロジックを使用してください。

将来タイルマップへ移行した際に、

```text
仮想グリッド
↓
実際の水タイル
```

へ差し替えやすい構造を優先してください。

---

## 今回の目的

このシステムによって、

```text
同じ場所で釣る
↓
その周辺の釣り圧が上がる
↓
魚影が徐々に減る
↓
少し場所を移動するメリットが生まれる
↓
時間が経てば元の場所も回復する
```

というゲームループを作る。

プレイヤーからはタイルや数値を意識させず、

**「この辺は釣りすぎて魚が薄くなってきたから、少し場所を変えよう」**

と自然に感じられることを目標としてください。