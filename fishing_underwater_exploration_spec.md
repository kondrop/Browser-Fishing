# 釣りゲーム：水中探索〜フッキング仕様

## 1. 概要

既存のファイトシステムは変更せず、その前段となる

`キャスト → 合わせ`

のフローを、新しい「水中探索パート」に置き換える。

新しい全体フローは以下。

```text
キャスト
↓
水中探索モーダルを開く
↓
針 / エサ / ルアーを操作して魚を探す
↓
魚へアピール
↓
魚がバイトモードへ移行
↓
フェイント / 本食いを見極める
↓
本食い中にフッキング
↓
フッキング成功
↓
探索モーダル終了
↓
既存ファイトシステム開始
↓
釣り上げ / 逃走
```

---

## 2. 基本方針

- 既存のファイトシステムはそのまま使用する
- 今回追加するのは「キャスト後〜フッキング成功まで」の探索パート
- 探索画面は水中環境を描画するモーダルとして表示する
- 水中環境の描画は、既存のアクアリウム機能で使用している表現・素材・ロジックを一部流用する
- 魚はイベント抽選ではなく、画面内をリアルタイムに泳ぐ「魚シンボル」として扱う
- プレイヤーは魚を探し、針 / エサ / ルアーを魚の前方へ持っていき、食わせる
- フッキング成功後のみ、既存ファイトへ遷移する

---

## 3. 探索画面の開始

### 3.1 開始条件

プレイヤーがキャストすると、水中探索モーダルを開く。

### 3.2 初期状態

- 針 / エサ / ルアーは浅い深度から開始する
- 魚シンボルを複数生成する
- 魚の数、種類、サイズ、初期深度はランダム
- 魚は画面内を上下左右に遊泳する

---

## 4. プレイヤー操作

### 4.1 左右キー

針 / エサ / ルアーの水平方向の位置を調整する。

```text
← : 左へ移動
→ : 右へ移動
```

### 4.2 上下キー

針 / エサ / ルアーの深度を調整する。

```text
↑ : 浅い方向へ移動
↓ : 深い方向へ移動
```

### 4.3 SPACEキー

現在の魚の状態によって役割が変わる。

#### 通常モード中

「アピール」として使用する。

- 針 / エサ / ルアーが短く「ちょん」と動く
- 魚の感知範囲内で使用した場合、その魚のアピール値増加にボーナスを与える

#### バイトモード中

「フッキング」として使用する。

- 本食い中に押した場合：フッキング成功
- フェイント中に押した場合：即失敗
- 本食い受付時間外に押した場合：成功扱いにはしない

---

## 5. 深度とファイト難易度

針が存在している深度は、フッキング成功後の既存ファイト難易度へ影響する。

### 基本ルール

- 浅い位置でフッキングした魚ほど、ファイト難易度は低い
- 深い位置でフッキングした魚ほど、ファイト難易度は高い

具体的な補正方法・倍率は実装後にテストしながら調整する。

フッキング成功時に、その時点の深度値を既存ファイト側へ渡せる構造にする。

例：

```ts
startFight({
  fish,
  hookDepth,
});
```

※ 実際の既存APIに合わせて変更すること。

---

# 6. 魚シンボル

## 6.1 生成

探索開始時に、複数の魚シンボルをランダム生成する。

魚ごとに最低限以下の情報を持つ。

```ts
type ExplorationFish = {
  id: string;

  fishType: string;
  size: number;

  position: {
    x: number;
    y: number;
  };

  direction: 'left' | 'right';

  state: FishExplorationState;

  appeal: number;
  appealThreshold: number;

  biteAttemptCount: number;
};
```

実際の型は既存の魚データ構造に合わせて統合する。

---

## 6.2 サイズ

魚シンボルの表示サイズは、実際の個体サイズによって変化する。

```text
小さい個体
→ 小さいシルエット

大きい個体
→ 大きいシルエット
```

魚種だけではなく、その個体のサイズ値を表示スケールへ反映する。

---

## 6.3 通常遊泳

通常モード中の魚の移動は、アクアリウムで使用している魚の遊泳挙動をベースにする。

基本挙動：

- 上下左右へ遊泳
- 一定時間ごとに方向や目標地点を変える
- 魚の向きに応じてシルエットを左右反転する
- 画面内を自然に移動する

アクアリウム側の既存実装を流用可能であれば、できるだけ共通化する。

---

# 7. 魚の状態

魚シンボルは内部的に状態を持つ。

推奨状態：

```ts
type FishExplorationState =
  | 'swimming'
  | 'bite'
  | 'hookWindow'
  | 'escaping'
  | 'hooked';
```

必要であれば `interested` 等を追加してもよいが、UI上で明示的なモード表示は不要。

---

# 8. 通常モード

## 8.1 概要

魚は通常遊泳しながら、針 / エサ / ルアーが自分の感知範囲へ入るのを監視する。

各魚シンボルは内部的に「アピール値」を持つ。

---

## 8.2 感知範囲

感知範囲は、

**魚の前方かつ魚の近く**

に設定する。

魚の背後に針があっても基本的には反応しない。

実装例：

- 前方楕円
- 前方扇形
- 前方矩形

など。

最初は実装しやすい形でよい。

感知距離・角度などの具体値は仮設定し、プレイテストで調整する。

---

## 8.3 アピール値

魚ごとに内部的な `appeal` 値を持つ。

```text
0
↓
魚の感知範囲内に針が存在
↓
時間経過で増加
↓
規定値到達
↓
バイトモードへ移行
```

### 感知範囲内

針が感知範囲内に存在する間、時間経過でアピール値を増加させる。

例：

```ts
appeal += baseAppealPerSecond * deltaTime;
```

### SPACEアピール

感知範囲内でSPACEを押すと、針が「ちょん」と動き、アピール値へボーナスを加える。

例：

```ts
appeal += spaceAppealBonus;
```

数値は仮値で実装し、プレイテストで調整する。

---

## 8.4 感知範囲外

感知範囲から外れた場合のアピール値は、即0には戻さない。

推奨：

- 時間経過でゆっくり減少する

例：

```ts
appeal -= appealDecayPerSecond * deltaTime;
appeal = Math.max(0, appeal);
```

具体的な減衰速度は調整前提。

---

# 9. バイトモード

## 9.1 移行条件

魚のアピール値が、その魚のアピール閾値を満たすとバイトモードへ移行する。

```ts
if (fish.appeal >= fish.appealThreshold) {
  enterBiteMode(fish);
}
```

バイトモードへ入ったことを示す専用UIやゲージ表示は行わない。

魚の行動変化だけでプレイヤーへ伝える。

---

## 9.2 バイトモード開始時の挙動

バイトモードへ移行すると、魚は通常遊泳を停止し、針 / エサ / ルアーをターゲットにする。

基本挙動：

1. 針へ近づく
2. 針との一定距離を保つ
3. 針を追従しながら様子を見る
4. 一定間隔で食いつき行動を行う

魚が針へ一直線に突っ込んだままになるのではなく、針の近くで一定距離を保つ。

---

# 10. 食いつき行動

## 10.1 最大試行回数

食いつき行動は最大5回。

```text
最大試行回数 = 5
```

1〜4回目は、ランダムで

- フェイント
- 本食い

のどちらかになる。

4回目まで全てフェイントだった場合、5回目は必ず本食いになる。

---

## 10.2 判定ルール

疑似コード：

```ts
function decideBite(attempt: number): 'feint' | 'real' {
  if (attempt >= 5) {
    return 'real';
  }

  return Math.random() < FEINT_RATE
    ? 'feint'
    : 'real';
}
```

フェイント率 `FEINT_RATE` は仮値で設定し、プレイテストで調整する。

---

# 11. 食いつきアニメーション

## 11.1 共通挙動

食いつき行動の前半は、フェイントか本食いかがすぐには分からないようにする。

例：

```text
一定距離で待機
↓
少し針へ寄る
↓
軽い予備動作
↓
針へアタック
```

---

## 11.2 フェイント

フェイント時は「ソフトタッチ」。

イメージ：

```text
少し距離を取る
↓
針へ寄る
↓
口先で軽く「ちょん」
↓
すぐ離れる
↓
待機距離へ戻る
```

プレイヤーがこのタイミングでSPACEを押した場合は即失敗。

---

## 11.3 本食い

本食い時は、フェイントより明確に「ガブッ」と食いつく。

イメージ：

```text
少し距離を取る
↓
針へ寄る
↓
最後に一段強く踏み込む
↓
ガブッと食いつく
↓
フッキング受付開始
```

視覚的には、

- 魚が大きめに踏み込む
- 針が引っ張られる
- ラインが一瞬張る

などを使用し、本食いだと認識できるようにする。

ただし、食いつき直前まではフェイントとある程度共通の動きにする。

---

# 12. 食いつき間隔

フェイント後、次の食いつき行動までランダムなインターバルを入れる。

毎回同じテンポにならないよう、

- すぐ次が来る
- 少し待ってから来る

の両方が発生する。

例：

```ts
nextBiteDelay = random(MIN_BITE_INTERVAL, MAX_BITE_INTERVAL);
```

具体値は仮設定し、プレイテストで調整する。

---

# 13. フッキング

## 13.1 本食い時

魚が本食いした瞬間から、一定時間だけフッキング受付時間を設ける。

```text
本食い
↓
HOOK WINDOW開始
↓
一定時間以内にSPACE
↓
成功
```

成功時：

1. 魚を `hooked` 状態にする
2. 探索操作を停止
3. 探索モーダルを終了
4. 対象魚の情報を既存ファイトへ渡す
5. フッキング時の深度を既存ファイトへ渡す
6. 既存ファイト開始

---

## 13.2 フェイント中のSPACE

フェイント中にSPACEを押した場合、

**即失敗**

とする。

現時点では救済処理を入れない。

将来的にスキル等で、

> 1回だけフェイントへの早合わせを無効化する

といった効果を追加する予定があるため、失敗判定は将来拡張しやすい構造にしておく。

例：

```ts
const canIgnoreFalseHook = playerSkills.falseHookGuard > 0;
```

※ 現段階では未実装でよい。

---

## 13.3 フッキング受付時間切れ

本食いしたにもかかわらず、一定時間内にSPACEが入力されなかった場合は失敗。

その魚シンボルは逃走状態へ移行する。

---

# 14. 失敗時の魚

以下の場合、その魚は失敗扱いとなる。

- フェイント中にSPACEを押した
- 本食い後、フッキング受付時間内にSPACEを押さなかった

失敗した魚は、

```text
逃走行動
↓
フェードアウト
↓
画面から削除
```

とする。

通常遊泳へは戻さない。

---

# 15. 魚のリポップ

探索画面には、最低2匹の魚シンボルが存在するようにする。

```ts
MIN_FISH_COUNT = 2;
```

魚が逃走・削除された結果、魚数が2匹未満になった場合、新しい魚を補充する。

補充魚は、

- 魚種：ランダム
- サイズ：ランダム
- 深度：ランダム

とする。

### 出現方法

可能であれば、

**画面外から泳いで入ってくる**

方法を優先する。

難しい場合は、画面端付近で短くフェードインさせてもよい。

画面中央への突然出現は避ける。

---

# 16. 探索画面の終了条件

探索画面が終了するのは以下の2パターン。

## 16.1 ESC

プレイヤーがESCを押した場合、

- 探索をキャンセル
- 探索モーダルを閉じる
- ファイトには移行しない

---

## 16.2 フッキング成功

フッキングに成功した場合、

- 探索モーダルを閉じる
- 対象魚を既存ファイトへ引き継ぐ
- フッキング時の深度を引き継ぐ
- 既存ファイトを開始する

---

# 17. 状態遷移

全体の魚状態遷移。

```text
┌──────────────┐
│   SWIMMING   │
│   通常遊泳    │
└──────┬───────┘
       │
       │ 感知範囲内で
       │ アピール値蓄積
       ▼
┌──────────────┐
│ appeal >=    │
│ threshold    │
└──────┬───────┘
       ▼
┌──────────────┐
│     BITE     │
│ バイトモード  │
└──────┬───────┘
       │
       ├── フェイント
       │      │
       │      ├─ SPACE → ESCAPING
       │      │
       │      └─ 未入力 → 次の試行
       │
       └── 本食い
              │
              ▼
       ┌──────────────┐
       │ HOOK WINDOW  │
       └──────┬───────┘
              │
              ├─ 時間内SPACE
              │      ↓
              │    HOOKED
              │      ↓
              │    既存ファイト
              │
              └─ 時間切れ
                     ↓
                  ESCAPING
                     ↓
                  フェードアウト
```

---

# 18. 探索全体フロー

```text
CAST
↓
探索モーダル起動
↓
針を浅い位置に生成
↓
魚シンボル生成
↓
上下左右で針を操作
↓
魚の前方感知範囲へ針を移動
↓
時間経過でアピール値増加
＋
SPACEで「ちょん」とアピールボーナス
↓
アピール閾値到達
↓
魚が針へ接近
↓
一定距離で追従
↓
食いつき試行
↓
┌─────────────┬─────────────┐
│ フェイント    │ 本食い       │
└──────┬──────┴──────┬──────┘
       │              │
 SPACE │              │ SPACE
       ▼              ▼
     失敗          フッキング成功
       │              │
       ▼              ▼
    逃走          探索モーダル終了
    消滅              │
                       ▼
                  既存ファイト
```

---

# 19. 仮パラメータ

以下は仕様確定値ではない。

最初は適当な値で実装し、プレイテストしながら調整する。

例：

```ts
const explorationConfig = {
  // 魚
  minFishCount: 2,

  // アピール
  baseAppealPerSecond: 10,
  spaceAppealBonus: 15,
  appealDecayPerSecond: 4,
  defaultAppealThreshold: 100,

  // SPACEアピール
  appealMotionDuration: 0.15,

  // バイト
  maxBiteAttempts: 5,
  feintRate: 0.55,
  minBiteInterval: 0.4,
  maxBiteInterval: 1.8,

  // フッキング
  hookWindowDuration: 0.6,

  // バイト時の距離
  biteStandbyDistance: 40,
};
```

この値を仕様として固定しないこと。

各パラメータはできるだけconfig側へ出し、コードを変更せず調整可能にする。

---

# 20. 実装上の推奨分離

既存ファイトを壊さないため、探索システムは独立したモジュールとして追加する。

例：

```text
fishing/
├─ exploration/
│  ├─ explorationController.ts
│  ├─ explorationFish.ts
│  ├─ explorationFishAI.ts
│  ├─ explorationInput.ts
│  ├─ explorationConfig.ts
│  └─ explorationRenderer.ts
│
├─ aquarium/
│  └─ 既存の魚遊泳・描画処理
│
└─ fight/
   └─ 既存ファイトシステム
```

実際のプロジェクト構成に合わせて調整する。

---

# 21. 既存ファイトとの接続

探索側から既存ファイトへ渡す最低限の情報：

```ts
type ExplorationResult = {
  fish: FishData;
  hookDepth: number;
};
```

フッキング成功時：

```ts
const result = {
  fish: targetFish.data,
  hookDepth: hook.position.y,
};

closeExploration();
startExistingFight(result);
```

既存ファイト側では、

- 対象魚
- 個体サイズ
- フッキング時の深度

を元に、既存の難易度計算へ必要な補正を追加する。

既存ファイトの操作ロジックそのものは変更しない。

---

# 22. 今回の実装対象

### 必須

- キャスト後に探索モーダルを開く
- 水中環境描画
- アクアリウム魚挙動の流用
- ランダム魚シンボル生成
- 個体サイズに応じたシルエットサイズ変更
- 上下左右による針操作
- SPACEによる「ちょん」アピール
- 魚前方の感知範囲
- アピール値
- バイトモード
- 針への接近・追従
- フェイント
- 本食い
- 最大5回の食いつき試行
- 5回目の本食い保証
- ランダムな食いつきインターバル
- SPACEによるフッキング
- フェイント早合わせの即失敗
- フッキング受付時間切れ
- 失敗魚の逃走・フェードアウト
- 最低2匹になるようリポップ
- ESCによる探索キャンセル
- フッキング成功時に既存ファイトへ遷移
- フッキング深度をファイトへ引き継ぐ

### 今回は不要 / 後回し

- 既存ファイトシステム自体の刷新
- フェイント早合わせを救済するスキル
- アピール値の最終バランス調整
- 感知範囲の最終バランス調整
- 各魚種固有の高度なバイトAI
- 深度によるファイト難易度補正値の最終調整

---

# 23. 実装時に重視すること

1. **既存ファイトシステムを壊さない**
2. 探索とファイトを明確に分離する
3. アクアリウムの魚挙動・水中描画をできるだけ再利用する
4. 魚の状態は内部ステートで管理する
5. アピール値などの内部数値を画面へ大量に表示しない
6. 「魚が反応しているか」は魚の動きで伝える
7. フェイントと本食いは、直前まで似た動きにする
8. 本食いは最終的な接触を「ガブッ」と分かりやすくする
9. 調整が必要な数値はconfigへ出す
10. 将来的なスキル追加を考慮して、フッキング失敗判定を拡張可能にしておく
