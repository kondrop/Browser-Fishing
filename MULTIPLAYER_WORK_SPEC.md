# 🌐 マルチプレイ機能 作業仕様書 (v3)

本書は、この釣りゲームにマルチプレイ機能を追加実装するための**作業仕様書**である。
実装を担当するAIエージェント（以下「実装者」）が、追加の聞き取りなしで着手できることを目的とする。

- 対象リポジトリ: `Browser-Fishing`（Phaser 3.90 + TypeScript + Vite）
- 関連文書: `ROADMAP.md`（フェーズ8）, `PROJECT_SHEET.md`, `BOOK_UI_STYLE_GUIDE.md`
- 本書のステータス: **承認済み仕様**。ROADMAP フェーズ8 の記載より本書を優先する。

### 更新履歴

| 版 | 内容 |
|----|------|
| v1 | Supabase（匿名認証+Realtime+Postgres）による常時稼働バックエンド案 |
| v2 | ホストプレイヤーPC担当の WebRTC P2P（PeerJS）案 |
| v3 | **オーナー決定により最終方針確定**: Node.js + WebSocket（Socket.io）サーバーを Render 無料枠にデプロイ。Render の自動スリープ/自動起動により「遊ぶときだけサーバー稼働」を無償で実現。コールドスタート（初回接続時 約1分）は許容と確認済み。WebRTC の社内ファイアウォール疎通リスクを排除 |

---

## 0. 決定済みの要件（発注者確認済み）

以下はプロダクトオーナーに確認済みの決定事項。実装者はこれを勝手に変更しないこと。

| 項目 | 決定内容 |
|------|---------|
| 利用形態 | **社内で遊ぶだけ。一般リリースはしない** |
| 規模 | 同僚 数人〜十数人が同じ釣り場で一緒に遊ぶ |
| サーバー | **常時稼働は不要**。Render 無料枠の自動スリープ（無通信15分で停止）/自動起動に任せる。**コールドスタート約1分は許容**（オーナー確認済み） |
| 部屋 | **部屋を作る → ルームコードを共有 → 他の人がコードで入室** |
| コスト | 完全無料（Render 無料 Web サービス + 無料静的ホスティング） |
| コア体験1 | 同じマップに他プレイヤーが見え、位置・向き・釣り動作（キャスト/待機/ファイト/釣果）が同期される |
| コア体験2 | **チャットは実装しない**。代わりに「**エール（拍手）**」機能: 魚を釣り上げたプレイヤーに賑やかなエフェクト付きの拍手を送れる。**送った側・送られた側の双方に経験値が入る** |
| コア体験3 | ランキング |
| アカウント | 不要。セーブデータは継続して遊べること → **既存の localStorage セーブがそのまま要件を満たす**（サーバー側のDB永続化は不要） |
| チート対策 | 低優先。社内の身内で遊ぶ前提のためクライアント信頼でよい |

### スコープ外（今回は実装しない）

- テキストチャット、ボイスチャット
- 協力釣り（ROADMAP 8-8）、対戦モード（8-9）、トレード（8-10）→ 将来拡張として設計上の余地だけ残す
- フレンドリスト（8-6）→ ルームコード共有で代替
- ログイン・アカウント（8-2）、DBによる永続化（8-3）→ 不要と決定
- 全期間・全ユーザー横断の永続グローバルランキング → DBが必要になるため見送り（Render 無料 Postgres は30日で失効し不向き）。**ルーム内ランキング**（§8）で代替

---

## 1. 現状のコードベース分析（実装者が最初に把握すべきこと）

### 1.1 構成

```
src/
├── main.ts                  # Phaser初期化（GameScene 1つのみ登録）
├── config.ts                # ゲームバランス設定
├── scenes/GameScene.ts      # ★ 約10,000行のモノリス。ゲームロジック・UI・入力すべてここ
├── data/
│   ├── inventory.ts         # PlayerData型・セーブ/ロード(localStorage)・経験値・釣果追加
│   ├── fish.ts / fishConfig.ts   # 魚マスタ（16種+ゴミ3種）
│   ├── skills.ts / questGenerator.ts / quests.ts / achievementConfig.ts など
├── fight/                   # ファイトミニゲームのロジック（分離済み）
├── ui/fishingGaugeOverlay.ts
└── debug/                   # バランスデバッグパネル
```

### 1.2 マルチプレイ観点で重要な事実

1. **釣りステートマシン**: `GameScene.ts` 冒頭の `FishingState`（`IDLE / CASTING / WAITING / BITE / FIGHTING / SUCCESS / FAIL`）。他プレイヤーの見た目再現はこの状態値+浮き座標があればほぼ足りる。
2. **セーブ**: `src/data/inventory.ts` の `savePlayerData()` / `loadPlayerData()` が `localStorage`（キー `fishingGame_playerData`）へ直書き。**v3ではセーブの仕組みは変更しない**（各プレイヤーのPCに保存されたまま）。
3. **キャラクター設定**: `bf_character_id` / `bf_player_name` / `bf_character_color` が個別の localStorage キーに保存（`GameScene.getSelectedCharacterId()` 等）。他プレイヤー表示に必要な「見た目3点セット」はこれ。
4. **釣果処理**: `inventory.ts` の `addFishToInventory()` が図鑑登録・経験値付与・レベルアップ判定まで一括で行う。「釣り上げた」イベントのフックポイントはここ（および GameScene 側の SUCCESS 遷移処理）。
5. **update ループ**: モーダル表示中もループは完全停止しない設計が既に入っている（GameScene 内コメント「オンラインマルチ対応のため完全停止はしない」約1948行付近）。
6. **UIはPhaserではなくDOM**: インベントリ・図鑑・ショップ等は HTML/CSS（nes.css + 自前トークン）のオーバーレイ。新規UI（部屋作成・ランキング等）も **Book UI スタイル（`.cursor/rules/book-ui-style-baseline.mdc`）に従うこと**。
7. **サーバー関連コードは一切存在しない**。`package.json` の依存は `phaser` と `nes.css` のみ。

### 1.3 最大のリスク: GameScene モノリス

GameScene.ts にネットワークコードを直接足すと確実に破綻する。**フェーズ0（リファクタリング）を必ず先に行う**こと（§4参照）。

---

## 2. 技術選定

### 2.1 確定構成

| 役割 | 技術 | 備考 |
|------|------|------|
| ゲームサーバー | **Node.js + TypeScript + Socket.io** | 部屋管理・メッセージ中継。リポジトリ内 `server/` ディレクトリ |
| サーバーホスティング | **Render 無料 Web サービス** | 無通信15分で自動スリープ、接続で自動起動（約1分）。プレイ中は WebSocket メッセージが流れている限りスリープしない（2026-02の仕様変更で確認済み） |
| クライアント配信 | **Render 静的サイト**（無料） | 同じダッシュボードで管理できるため。GitHub Pages / Vercel でも可 |
| セーブ | 既存の localStorage | 変更なし |
| DB | **使わない** | 部屋状態はサーバーのメモリ内のみ（消えてよいデータしか置かない） |

**Socket.io を選ぶ理由**（素の `ws` との比較）:
- 自動再接続・ハートビート・部屋（room）機能が組み込みで、Render の「サーバーはいつでも再起動されうる」性質（§9-6）への対処が楽。
- クライアント/サーバー双方で同じイベント名ベースのAPIになり、プロトコル定義（§3.3）を型で共有しやすい。
- 依存を最小にしたい場合は素の `ws` でも成立するが、再接続・部屋管理を自前実装することになるため推奨しない。

### 2.2 コスト・無料枠の根拠（2026年7月時点で確認済み）

- Render 無料 Web サービス: **750インスタンス時間/月**。スリープ中は消費しないため、「夕方に数時間」用途では上限に達しない。
- スリープ条件: HTTP リクエストも WebSocket メッセージも15分間届かないこと。**プレイ中は position 同期が流れ続けるため落ちない**。全員が抜けて15分で自動停止 = 「やめたら終了」が自動で実現される。
- 帯域: ワークスペースの月間無料枠（現行プランで月100GB。セットアップ時にダッシュボードで要確認）に対し、§3.2 の送信最適化（変化時のみ送信+サーバー側バッチ配信）を行えば、釣りゲームはプレイヤーが静止している時間が長いため実測は月数GBに収まる見込み。**超過時は支払い方法未登録なら課金ではなくサービス停止**なので、事故っても金銭リスクはない。

### 2.3 対抗案（記録）

| 案 | 評価 |
|----|------|
| WebRTC P2P + ホストPC担当（v2案） | サーバー不要だが、社内ファイアウォール/VPN で WebRTC が疎通しないリスクがあり、ホスト退出=部屋解散の制約もある。オーナーが「ホストPC方式に固執しない」と明言したため取り下げ |
| Cloudflare Workers + Durable Objects | コールドスタートなしだが、無料枠が受信メッセージ数ベース（10万リクエスト相当/日、WSメッセージは20:1換算）のため高頻度同期で上限に当たりうる。コールドスタート許容の確認が取れたため不採用 |
| Supabase Realtime（v1案） | 成立するが、外部SaaSアカウント管理と1週間無アクセスでのプロジェクト一時停止が煩雑。自前サーバーの方が部屋ロジックを素直に書ける |

### 2.4 抽象化の必須要件

将来の移行や機能拡張（協力プレイの権威ロジック等）に備え、**GameScene からは Socket.io を直接触らせない**。
`src/net/` 配下に以下のインターフェースを定義し、Socket.io 実装はその実装詳細とする:

```ts
// src/net/types.ts（イメージ。命名は実装者に委ねる）
export interface NetClient {
  /** 部屋を作る。戻り値はサーバーが発行したルームコード */
  createRoom(profile: PlayerProfile): Promise<string>;
  /** ルームコードで入室 */
  joinRoom(roomCode: string, profile: PlayerProfile): Promise<void>;
  leaveRoom(): void;
  sendState(state: NetPlayerState): void;          // 自分の位置・釣り状態（スロットルはNetClient側の責務）
  sendEvent(ev: NetGameEvent): void;               // 釣果・エールなど単発イベント
  onSnapshot(cb: (snap: RoomSnapshot) => void): void;
  onPeerEvent(cb: (peerId: string, ev: NetGameEvent) => void): void;
  onPeerJoin(cb: (peer: PeerInfo) => void): void;
  onPeerLeave(cb: (peerId: string) => void): void;
  onConnectionChange(cb: (s: 'connecting' | 'waking' | 'online' | 'offline') => void): void;
}
```

---

## 3. アーキテクチャ全体像

```
[ブラウザ A]──┐
[ブラウザ B]──┼── wss ──► [Render 無料 Web サービス]
[ブラウザ C]──┘            Node + Socket.io
                            ├─ GET /healthz     … 起床用・死活監視
                            ├─ rooms: Map<code, Room>   … メモリ内のみ
                            │    Room = { members: Map<socketId, PlayerProfile>,
                            │             lastStates: Map<socketId, NetPlayerState> }
                            ├─ 8Hz でルームごとに snapshot をバッチ配信
                            └─ event（釣果/エール）は即時中継 + 軽い検証
```

### 3.1 部屋のライフサイクル

1. **部屋を作る**: クライアントが `create-room` を送信 → サーバーがルームコード（6文字英数字、紛らわしい文字 `0/O/1/I/l` は除外）を発行して部屋を生成 → 作成者を入室させ、コードを返す。画面にコードを大きく表示（クリックでコピー）。**部屋主に特別な役割はない**（作成者が抜けても部屋は残る）。
2. **入室**: `join-room {code, profile}` → 存在しないコードなら `room-not-found`、満室（**16人**）なら `room-full` を返す。成功時はサーバーが現在のメンバー一覧を返し、他メンバーへ `peer-join` を配信。
3. **退出/切断**: ソケット切断で自動退出。他メンバーへ `peer-leave` 配信。**メンバーが0人になった部屋は即削除**。
4. **サーバー再起動・スリープからの復帰**: 部屋はメモリ内のみなので消える。クライアントは「入室中のルームコード」を保持し、**Socket.io の再接続完了時に自動で `join-room` をやり直す**（部屋が消えていれば同じコードで `create-room` を試みる。これにより全員が自動再結集できる）。この自動再入室は必須要件（Render 無料枠は「いつでも再起動されうる」ため）。

### 3.2 同期方式（クライアント主導・サーバーは中継と検証のみ）

**上り（クライアント→サーバー）:**
- 自分の state を**変化があったときのみ最大10Hz**で送信。位置・状態が不変の間は**3秒に1回**のキープアライブのみ（釣りゲームは静止時間が長いので、これが帯域とスリープ判定の両方に効く）。

**下り（サーバー→クライアント）:**
- サーバーはルームごとに**8Hzのタイマーで全員分の最新 state をまとめた snapshot を一括配信**する（1メッセージに全員分。メッセージ数と帯域のオーバーヘッド削減）。変化がない tick は配信をスキップする。
- event（釣果・エール等）は snapshot に載せず**即時中継**（体感遅延を最小化）。

**受信側の表示:**
- `RemotePlayerManager` が補間表示: 直近2サンプルの線形補間（150ms程度の表示遅延バッファ）。
- `FishingState` 遷移は state に含めつつ、**SUCCESS（釣果）は単発イベント `catch` としても送る**（演出・エールのトリガー用）。
- **最後の受信から5秒 state が来ないピアは半透明化、15秒で削除**（タブ放置対策。ブラウザは非アクティブタブの描画ループを止めるため送信が途絶える。「消える」挙動で吸収する）。

### 3.3 メッセージプロトコル（v1）

型定義はクライアント/サーバーで共有するため **`shared/protocol.ts`** に置く（クライアントは `src/net/` から、サーバーは `server/src/` から import。Vite/tsc 双方から参照できる位置にすること）。

すべてのメッセージに `v: 1`（プロトコルバージョン）を含める。**互換を壊す変更時は v を上げ、不一致メッセージは黙って捨てる**（リロード前後の新旧クライアント混在対策）。

```ts
// state（上り: 変化時のみ最大10Hz / 不変時 1/3Hz）
interface NetPlayerState {
  v: 1;
  x: number; y: number;           // プレイヤー座標（整数に丸めて送る）
  facing: 'up'|'down'|'left'|'right';
  fs: number;                     // FishingState 値
  bx?: number; by?: number;       // 浮き座標（WAITING/BITE/FIGHTING時のみ）
}

// snapshot（下り: 8Hz バッチ。id は Socket.io の socketId）
interface RoomSnapshot {
  v: 1;
  t: number;                       // サーバー時刻(ms)。補間のデバッグ用（補間自体はローカル受信時刻基準）
  states: Record<string, NetPlayerState>;
}

// event（単発・即時中継）
type NetGameEvent =
  | { v: 1; t: 'catch'; seq: number; fishId: string; size?: number; rarity: string }
  | { v: 1; t: 'cheer'; targetId: string; catchSeq: number }
  | { v: 1; t: 'levelup'; level: number }
  | { v: 1; t: 'stats'; stats: RoomStats };   // ルーム内ランキング用（§8）

// profile（入室時とプロフィール変更時）
interface PlayerProfile {
  v: 1;
  name: string;          // bf_player_name（未設定時 "つりびと" 等のデフォルト。12文字まで。サーバー側でも切り詰める）
  characterId: string;   // bf_character_id
  color: string;         // bf_character_color（#rrggbb 形式をサーバー側で検証）
  level: number;
  stats: RoomStats;      // 生涯統計（ランキング初期表示用）
}

interface RoomStats {
  level: number;
  totalCaught: number;
  maxFishSize: number;
  totalMoneyEarned: number;
  cheersReceived: number;   // PlayerData に累計列を追加する（§7）
  sessionCaught: number;    // 入室後の釣果数（「今日の釣り大会」表示用）
}
```

**プレイヤー識別**: セッション内IDとして socketId を使う。永続IDは不要（アカウントなし・ルーム内ランキングのみのため）。再接続で socketId が変わるのは許容（自動再入室で join し直すため、他者からは「一瞬抜けて戻った」ように見える）。

### 3.4 コールドスタートのUXフロー（必須実装）

Render がスリープ中の場合、最初の接続に約1分かかる。以下のフローで体験を担保する:

1. 「部屋を作る/入る」操作時、まず `GET /healthz` を叩く（タイムアウト5秒）。
2. 応答がなければ「🎣 サーバーを起こしています…（最大1分ほどかかります）」の表示を出し、**5秒間隔で `/healthz` をリトライ**（最大90秒）。この最初のリクエスト自体が Render の起床トリガーになる。
3. 応答が返ったら WebSocket 接続 → 部屋処理へ。
4. 90秒超えたら「サーバーに接続できませんでした。時間をおいて再試行してください」を表示。
- 待機中もソロプレイは継続できること（モーダルでゲームをブロックしない）。

---

## 4. フェーズ0: 事前リファクタリング（マルチ実装前に必須）

**目的**: GameScene に手を入れずにネットワーク層を追加できる構造にする。**このフェーズではゲームの挙動を一切変えないこと**（リグレッションゼロが完了条件）。

| No. | 作業 | 詳細 |
|-----|------|------|
| 0-1 | **釣果イベントの発行点を一本化** | 「釣り上げ成功」が確定する箇所（SUCCESS遷移 + `addFishToInventory` 呼び出し）を1つのメソッドに集約し、そこでゲーム内イベント（`this.events.emit('fish-caught', ...)` 等）を発行する。ネット層はこれを購読する |
| 0-2 | **プレイヤー見た目の描画分離** | 自機スプライトの生成・着色・アニメ再生・竿/糸/浮き描画のうち、**「見た目の再現」に必要な部分**を `PlayerAvatar` クラス（仮）として切り出し、自機と他プレイヤーの両方から使えるようにする。GameScene の全面改修は不要。最低限「characterId + color + 状態 → 見た目」が独立関数になっていればよい |
| 0-3 | **プロフィール取得の集約** | `bf_character_id`/`bf_player_name`/`bf_character_color` の読み書きを1モジュール（例 `src/data/profile.ts`）へ集約する |

**完了条件**: `npm run build` が通り、手動プレイで釣り→売却→レベルアップ→セーブ→リロード復元が既存どおり動くこと。

---

## 5. フェーズM1: サーバー構築・接続基盤・部屋UI

### 5.1 サーバー（`server/` ディレクトリ新設）

1. 構成: `server/package.json`（独立管理）+ TypeScript + `socket.io`。HTTPフレームワークは不要（`node:http` + Socket.io で足りる。`/healthz` だけ素のハンドラで返す）。
2. 実装内容:
   - `create-room` / `join-room` / 切断処理と部屋管理（§3.1）。
   - state 受信 → ルームの `lastStates` 更新 → 8Hz snapshot バッチ配信（§3.2）。
   - event の即時中継 + 検証（§7.2）。
   - **受信メッセージは全て型ガードで検証**し、不正なら黙って捨てる（クラッシュ厳禁）。1接続あたりの受信レート上限（例: 30msg/秒）を超えたら切断。
   - CORS: Socket.io の `cors` オプションでクライアント配信オリジンのみ許可（環境変数 `ALLOWED_ORIGIN`）。
   - ポートは `process.env.PORT`（Render が注入する）。
3. ローカル開発: `server/` で `npm run dev`（tsx watch 等）、クライアントは `VITE_SERVER_URL=http://localhost:3001` で接続。ルートの `package.json` に `dev:server` スクリプトを追加してよい。
4. デプロイ: リポジトリに `render.yaml`（Blueprint）を置き、`server/` をルートディレクトリとする無料 Web サービスと、クライアントの静的サイト（`npm run build` → `dist/`）を定義する。人間向け手順を `docs/DEPLOY.md` に書く（Render アカウント作成〜Blueprint 適用〜`VITE_SERVER_URL` の設定まで）。

### 5.2 クライアント側

1. `socket.io-client` を依存に追加（クライアントの追加依存はこれ1つの想定。他を足す場合は理由をPR説明に明記）。
2. `src/net/` に §2.4 の `NetClient` と Socket.io 実装、§3.4 の起床フロー、§3.1-4 の自動再入室を実装。
3. 部屋UI（Book UI スタイル準拠）:
   - タイトルまたは設定モーダルに「マルチプレイ」セクション: 「部屋を作る」「部屋に入る（コード入力）」「退出」。
   - 接続中は画面隅に常時表示: ルームコード / 参加人数 / 接続状態（connecting / waking / online / offline）。
   - エラー表示: 部屋が見つからない / 満室 / サーバー起床タイムアウト / 切断→再接続中。
4. **未入室・接続失敗時は従来と完全に同一の挙動**であること（ソロプレイに一切影響を与えない）。

### 5.3 完了条件

- ローカルでサーバーを起動し、2ブラウザ（通常+シークレット）で部屋作成→入室→join/leave イベント相互受信→退出が動く。
- Render にデプロイし、スリープ状態から §3.4 のフローで起床→接続できる。サーバーを手動再起動しても両クライアントが自動再入室で同じ部屋に戻る。

---

## 6. フェーズM2: 他プレイヤー表示（ルーム同期）

### 6.1 作業項目

1. `RemotePlayerManager` を新設（`src/net/remotePlayerManager.ts` 等。GameScene 外）:
   - join/leave で他プレイヤーのアバター（フェーズ0-2 の `PlayerAvatar`）を生成/破棄。
   - snapshot 受信 → 補間バッファへ（§3.2）。5秒未受信で半透明化、15秒で削除。
   - 他プレイヤーの浮き・釣り糸も簡易描画（`fs` と `bx/by` から）。ファイトミニゲームUIは他人には見せない。
   - 他プレイヤーに当たり判定は付けない（すり抜け。釣り座の取り合い問題を回避）。
   - 名前ラベル（`name` + レベル）を頭上表示。自機と見分けやすい配色にする。
2. 自分の state 送信: GameScene の update から `NetClient.sendState()` を呼ぶ（変化検知・間引きは NetClient 側の責務）。
3. 他プレイヤーの `catch` イベント受信時、頭上に釣果表示（魚アイコン+サイズ）を数秒出す（M3のエール導線の土台）。

### 6.2 完了条件

- 2ブラウザで互いの移動・キャスト・釣り上げが視認でき、補間が滑らか（カクつき・ワープがない）。
- 片方をリロード/タブ閉じしても、もう片方でアバターが正しく消える・再入室で復帰する。
- 静止中の上り送信が 1/3Hz に落ちていることをデバッグ表示で確認できる。

---

## 7. フェーズM3: エール（拍手）機能 ★本プロジェクトの目玉

### 7.1 体験仕様

1. 他プレイヤーが魚を釣り上げると（`catch` イベント受信）、自分の画面端に「🎉 ○○さんが △△ を釣り上げた！ [エールを送る]」というトースト通知が出る（表示時間: 8秒）。
2. トーストのボタン（またはショートカットキー、例: `E`）でエール送信（`cheer` イベント）。
3. エールを受けた側の画面では、自分の周囲に**賑やかな拍手エフェクト**（拍手の手アイコン+紙吹雪。ドット絵調で既存UIの雰囲気に合わせる）が再生され、「○○さんからエール！ +XX EXP」と表示。送った側にも小さな確認演出。
4. `cheer` はルーム全員に配信し、第三者の画面でも対象プレイヤーの周囲にエフェクトが見える（誰宛かは `targetId` で判別）。
5. **経験値**（`config.ts` に `multiplayer` セクションを新設して定数化）:
   - 受け取った側: 釣った魚のレア度基準EXPの **20%**（最低1）。
   - 送った側: 固定 **2 EXP**。
   - 付与は各クライアントが自分の分を `addExp()` で加算する（クライアント信頼。チート対策低優先の決定どおり）。
6. 同一の釣果（`catchSeq` で識別）に対して各プレイヤーが送れるエールは**1回**。トースト消滅後は送れない。
7. `PlayerData` に `cheersSent` / `cheersReceived`（累計）を追加し、セーブ対象にする（実績・ランキング用）。既存セーブとの互換は `loadPlayerData()` の既存パターン（デフォルト値マージ）に従う。

### 7.2 スパム防止（サーバー側で検証）

サーバーが存在するので、エールの検証はサーバー側で行う（数行で済み、クライアント側の重複実装より確実）:

- 同一送信者→同一対象は **10秒に1回**まで。超過分は中継しない。
- 同一 `catchSeq` への同一送信者からの重複は中継しない。
- 自分自身宛は中継しない。
- 検証状態はメモリ内でよい（部屋が消えれば忘れてよい）。

### 7.3 完了条件

- 2ブラウザ間で「釣る→トースト→エール→双方EXP加算→拍手エフェクト」が一連で動く。3窓目でも第三者視点でエフェクトが見える。
- 連打・重複送信で2重加算されない（サーバーログで棄却を確認）。未入室時はエールUI自体が出ない。

---

## 8. フェーズM4: ルーム内ランキング

グローバルランキングはDBが必要なため見送り（§0）。**同じ部屋にいるメンバー間のランキング**を実装する。

1. 各プレイヤーは入室時に `PlayerProfile.stats`（生涯統計）を送り、以後は値が変わったとき（釣果・売却・エール受領時）に `stats` イベントで更新を配信する（最短5秒間隔にスロットル）。
2. UI: 統合Book UI に「ランキング」タブを追加（`unifiedBookTab` の型に追加。既存タブ実装のパターンと Book UI スタイルに従う）。
   - 部門: 総釣果 / レベル / 最大サイズ / 累計獲得金額 / 受け取ったエール数 / **今日の釣果（入室後の `sessionCaught`。釣り大会的な楽しみ方用）**。
   - 自分の行をハイライト。未入室時はタブをグレーアウトし「部屋に入ると表示されます」。
3. 退出したメンバーはランキングから消えてよい（部屋にいる人だけの対抗戦とする）。

**完了条件**: 3窓で入室し、釣果に応じて各部門の順位が全員の画面で一致して更新される。

---

## 9. 実装上の注意点（全フェーズ共通）

1. **ソロプレイを壊さない**: 未入室・接続失敗・サーバー停止時に一切の機能低下がないこと（マルチ機能が無効になるだけ）。各フェーズの受け入れ条件に含める。
2. **GameScene への追加は最小限に**: ネットワーク関連の新規コードは `src/net/` へ。GameScene には「イベント発行」「update からの数行の呼び出し」以上を書かない。
3. **型の一元化**: プロトコル型は `shared/protocol.ts` に集約し、クライアント/サーバーで共有。イベント名のマジックストリングを散らさない。
4. **プロトコル互換**: `v` 不一致のメッセージは黙って捨てる。新旧クライアント混在で落ちないこと。
5. **不正データ耐性**: サーバー/クライアント双方で、受信データはすべて不正な形の可能性がある前提で型ガードしてからパースする。壊れたメッセージ1つでクラッシュしないこと。
6. **サーバーは「いつでも消える」前提で書く**: Render 無料枠はいつでも再起動されうる・スリープで全メモリが消える。サーバーに置いてよいのは「消えても遊び直せる」データのみ（部屋・最新state・エール検証状態）。永続データは置かない。自動再入室（§3.1-4）で復旧できる設計を崩さないこと。
7. **パフォーマンス/帯域**: 上りは変化時のみ+スロットル、下りは snapshot バッチ（§3.2）。座標は整数化。受信ハンドラ内で毎回 DOM 生成しない（トースト等は再利用）。`PERFORMANCE_ANALYSIS.md` の既存知見を踏襲。
8. **秘密情報なし構成を維持**: 本構成にAPIキー等の秘密は存在しない（`VITE_SERVER_URL` は公開値）。今後も service key 類をクライアントに含めない。
9. **時刻**: クライアント時計のズレを前提に、補間はローカル受信時刻ベースで行う。
10. **コミットメッセージは日本語**（リポジトリ規約）。

---

## 10. テスト・検証方法

- **多窓テスト**: 通常+シークレット+別ブラウザ（Chrome/Safari等）の3窓を基本検証とする。部屋作成・入室・満室・退出・再入室の各パス。
- **サーバー再起動テスト**: プレイ中に Render でサーバーを手動再起動（またはローカルでプロセス kill）→ 全クライアントが自動再入室で同じ部屋に再結集すること。**本構成で最も重要なテスト**。
- **コールドスタートテスト**: スリープ状態（15分放置後）から §3.4 のフローで起床→接続。待機表示とタイムアウト表示の確認。
- **回線劣化**: Chrome DevTools の Network throttling（wss にも有効）で補間破綻・エール2重送信がないか確認。
- **タブ非アクティブ**: 片方を5分放置 → 他方でフェード→削除 → 復帰で再表示。
- **不正メッセージ耐性**: デバッグコンソールから壊れたJSON/型不一致メッセージを注入してもサーバー・クライアントともクラッシュしないこと。
- **自動テスト**: プロトコルの型ガード・補間ロジック・サーバーの部屋管理とエール検証は純関数/独立モジュールに切り出して単体テストを書く（テスト基盤未導入のため `vitest` の導入をM1で行ってよい。サーバー側も同様）。

---

## 11. 作業順序と目安

| 順序 | フェーズ | 内容 | 規模感 |
|------|---------|------|--------|
| 1 | フェーズ0 | リファクタリング（挙動変更なし） | 中 |
| 2 | M1 | サーバー構築 + 接続基盤 + 部屋UI + Render デプロイ | 大 |
| 3 | M2 | 他プレイヤー表示 | 大 |
| 4 | M3 | エール機能 | 中 |
| 5 | M4 | ルーム内ランキング | 小 |

- 各フェーズは独立して main にマージ可能な単位とし、末尾の「完了条件」をPRの受け入れ条件とする。
- M1 の Render デプロイ（アカウント作成・Blueprint適用）は人間の作業を含むため、実装者は `docs/DEPLOY.md` に手順書を用意し、デプロイ確認をオーナーに依頼すること。ローカルサーバーでの検証まで済ませてあれば、デプロイ確認を待たずに M2 へ進んでよい。
- 着手前に `ROADMAP.md` フェーズ8 の表を本書に合わせて更新すること（8-1→Node+Socket.io/Render無料枠、8-2/8-3→不要と決定、8-4→M2、8-5→エール機能に置換、8-7→ルーム内ランキング）。

---

## 12. 将来拡張のための設計メモ（今回は実装しない）

- **協力釣り/対戦**: サーバーに部屋ごとの権威ロジックを足せばよい（本構成は自然に拡張できる。v2のP2P案より圧倒的に楽）。
- **複数釣り場（ROADMAP フェーズ5）**: state に `areaId` を足し、同エリアのプレイヤーのみ描画すればよい。プロトコル拡張余地として認識しておく。
- **グローバルランキング/クラウドセーブ**: 必要になったら外部の無料DB（Supabase 等。v1仕様書の設計が流用可能）をサーバーから利用する。Render 無料 Postgres は30日で失効するため使わないこと。
- **トレード**: 実装するならサーバー権威でのアトミック処理が必須。クライアント信頼では実装しないこと。

---

## 13. 未決事項（実装者が着手時にオーナーへ確認 or 妥当なデフォルトで進めてよい項目）

| 項目 | デフォルト案 |
|------|-------------|
| 部屋UIの置き場所（タイトル画面 or ゲーム内設定） | ゲーム内から開ける設定モーダル内（現状タイトル画面が薄いため） |
| エールのエフェクト具体演出（音の有無。SEは未実装領域） | 音なし・視覚演出のみで開始 |
| ルームコードの桁数・文字種 | 6文字英数字（紛らわしい文字除外） |
| セッションランキング（今日の釣果）のリセットタイミング | 退出でリセット（部屋にいる間のみカウント） |
| クライアントの配信先（Render静的サイト or 既存の配信方法） | Render 静的サイト（サーバーと同一ダッシュボード管理） |
